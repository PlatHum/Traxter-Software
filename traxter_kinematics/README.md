## traxter_kinematics Package

Currently this package is made up of only one node. It listens to the ```/cmd_vel``` topic and transforms it into the appropriate commands for the real robot or the simulated one.