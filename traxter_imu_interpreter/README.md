## imu_interpreter Package

This package just reads the imu information coming from the low-level, adds the respective covariance to each measurement and publishes it in the correct IMU ROS message format.